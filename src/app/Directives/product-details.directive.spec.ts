import { ProductDetailsDirective } from './product-details.directive';

describe('ProductDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
