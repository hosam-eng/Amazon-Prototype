import { ScroolpageDirective } from './scroolpage.directive';

describe('ScroolpageDirective', () => {
  it('should create an instance', () => {
    const directive = new ScroolpageDirective();
    expect(directive).toBeTruthy();
  });
});
